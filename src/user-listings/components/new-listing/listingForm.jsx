import React from "react";

export const ListingForm = () => {
  return (
    <div>
      name, description, address, price, bathrooms, bedrooms, type, mode(select of sale and rent), amenities(array for them to add as many aas the want),
    </div>
  );
};
